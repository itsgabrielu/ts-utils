import { expect } from "chai";

describe("Linked Lists", () => {
  [undefined].forEach(() => {
    describe(`For xxx`, () => {
      it("should know 2 equals 2", () => {
        expect(2).to.equal(2);
      });
    });
  });
});
